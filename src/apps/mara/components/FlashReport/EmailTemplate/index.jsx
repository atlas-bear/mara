import React from 'react';
import { styles } from './styles';

const EmailTemplate = ({ incident }) => {
  return (
    <div>
      {/* TODO: Add email template component */}
    </div>
  );
};

export default EmailTemplate;